// Signup page metadata
export const metadata = {
  title: 'Join BookFlix — Create Your Account',
  description: 'Sign up for BookFlix and start renting books from our curated catalog.',
}

export default function SignupLayout({ children }) {
  return children
}
